/**
 * The `IPauseFeature` interface represents a feature that pauses the player.
 */
export interface IPauseFeature {
  /**
   * Executes the logic to pause the playback of the player.
   */
  execute(): void;
}
